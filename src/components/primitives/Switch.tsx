interface SwitchProps { disabled?: boolean; checked: boolean; onChange: (checked: boolean) => void; labelledBy: string; describedBy?: string; }
export const Switch = ({ checked, onChange, labelledBy, describedBy, disabled }: SwitchProps) => <button type="button" role="switch" disabled={disabled} aria-checked={checked} aria-labelledby={labelledBy} aria-describedby={describedBy} onClick={() => onChange(!checked)} className={`disabled:opacity-50 disabled:cursor-not-allowed app-switch ${checked ? 'app-switch-on' : ''}`}><span aria-hidden="true" /></button>;
